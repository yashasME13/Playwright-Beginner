git clone <repository-url>
git status

#to push chnages to git repository
git add .
git commit -m "commit message"
git push origin main

#to pull code from git repository
git pull origin main
then make changes and push the code